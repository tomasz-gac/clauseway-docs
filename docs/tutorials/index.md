# Tutorials

The tutorial track is being written. Planned, in reading order:

1. **Your first relation** — goals, unification, `solve`.
2. **Domains and labelling** — finite domains, propagation, search.
3. **Arithmetic that runs backwards** — `addo` over ints, dates and exact decimals.
4. **Tabling** — recursion that terminates, answers that cache.
5. **Data as relations** — pldb sources, pushdown and transactions.
6. **The library walkthrough** — a full domain: policies, denials, premises.

!!! note
    Each tutorial lands when its text has been reviewed — this page
    tracks what exists rather than promising dates.
